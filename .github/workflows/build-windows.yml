name: 'Windows'

on:
  workflow_dispatch:
    inputs:
      upload_game:
        type: boolean
        description: 'Upload game'
        required: false
        default: false
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: 'Build for Windows'
    runs-on: windows-latest
    env:
      SDL3_PATH: ${{github.workspace}}/SDL3
      SDL3_IMAGE_PATH: ${{github.workspace}}/SDL3_image
      GAME_PATH: ${{github.workspace}}/game

    steps:
      - uses: actions/checkout@v3

      - name: 'Build SDL3'
        run: |
          git clone https://github.com/libsdl-org/SDL.git -b main
          cd SDL
          mkdir build
          cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=${{ env.SDL3_PATH }}
          cmake --build . --config Release -j 6
          cmake --install . --config Release

      - name: 'Build SDL3_image'
        run: |
          git clone https://github.com/libsdl-org/SDL_image.git -b main
          cd SDL_image
          mkdir build
          cd build
          cmake .. -DCMAKE_PREFIX_PATH=${{ env.SDL3_PATH }} -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=${{ env.SDL3_IMAGE_PATH }}
          cmake --build . --config Release -j 6
          cmake --install . --config Release

      - name: 'Re-link libraries'
        run: |
          ldd ${{ env.SDL3_IMAGE_PATH }}/bin/SDL3_image.dll
          export PATH=$PATH:${{ env.SDL3_PATH }}/bin:${{ env.SDL3_IMAGE_PATH }}/bin
          ldconfig
          ldd ${{ env.SDL3_IMAGE_PATH }}/bin/SDL3_image.dll

      - name: 'Build'
        run: |
          cmake -DCMAKE_PREFIX_PATH="${{ env.SDL3_PATH }};${{ env.SDL3_IMAGE_PATH }}" -DCMAKE_INSTALL_PREFIX=${{ env.GAME_PATH }} -DCMAKE_BUILD_TYPE=Debug -S ${{github.workspace}} -B ${{github.workspace}}/cmake-build-debug
          cmake --build ${{github.workspace}}/cmake-build-debug --target game -j 6
          cmake --install ${{github.workspace}}/cmake-build-debug --config Debug

      - name: 'Upload game'
        if: github.event.inputs.upload_game == 'true' || github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v3.1.0
        with:
          name: 'game_win64_${{github.run_number}}'
          path: '${{ env.GAME_PATH }}'
